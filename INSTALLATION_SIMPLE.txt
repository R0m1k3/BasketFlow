═══════════════════════════════════════════════════════════════
  🏀 BASKET FLOW - INSTALLATION SERVEUR PRIVÉ
═══════════════════════════════════════════════════════════════

✅ SOLUTION ADOPTÉE : init.sql crée tout automatiquement

═══════════════════════════════════════════════════════════════

📦 FICHIERS À TRANSFÉRER
───────────────────────────────────────────────────────────────

Racine :
  • docker-compose.yml
  • init.sql
  • README_SERVEUR.md

Backend :
  • backend/Dockerfile
  • backend/package.json
  • backend/prisma/schema.prisma
  • backend/src/ (tous les fichiers)

Frontend :
  • frontend/ (tous les fichiers modifiés)

═══════════════════════════════════════════════════════════════

🚀 INSTALLATION (3 commandes)
───────────────────────────────────────────────────────────────

cd /chemin/vers/basket-flow

docker network create nginx_default

docker-compose up -d


Attendez 30 secondes.

═══════════════════════════════════════════════════════════════

🔐 TESTER
───────────────────────────────────────────────────────────────

http://localhost:4000/login

admin / admin

═══════════════════════════════════════════════════════════════

✅ CE QUI SE PASSE AUTOMATIQUEMENT
───────────────────────────────────────────────────────────────

1. PostgreSQL démarre
   → init.sql s'exécute automatiquement
   → Crée toutes les tables
   → Crée l'admin (admin/admin)

2. Backend démarre
   → Prisma Client généré (query builder uniquement)
   → PAS de migrations
   → Serveur sur port 3888

3. Frontend démarre
   → Port 4000

═══════════════════════════════════════════════════════════════

🎯 POURQUOI CETTE SOLUTION
───────────────────────────────────────────────────────────────

✓ init.sql = Standard PostgreSQL (fiable à 100%)
✓ Prisma = Query builder seulement (pas de migrations)
✓ Automatique = Aucune commande manuelle
✓ Simple = Ça marche toujours

═══════════════════════════════════════════════════════════════

📖 Documentation complète : README_SERVEUR.md

═══════════════════════════════════════════════════════════════
