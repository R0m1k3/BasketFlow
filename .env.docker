# Configuration Docker - Basketball App
# ATTENTION: Ne jamais commiter ce fichier dans Git!

# API Basketball (optionnel - utilise des données d'exemple si vide)
API_BASKETBALL_KEY=

# Secrets de sécurité (OBLIGATOIRES)
# Générez-les avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=
SESSION_SECRET=

# Base de données Docker
DATABASE_URL=postgresql://basketuser:basketpass@postgres:5432/basketdb

# Environnement
NODE_ENV=production
PORT=3000
